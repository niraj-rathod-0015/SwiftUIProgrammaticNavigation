import XCTest
@testable import SwiftUIProgrammaticNavigation

final class SwiftUIProgrammaticNavigationTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftUIProgrammaticNavigation().text, "Hello, World!")
    }
}
